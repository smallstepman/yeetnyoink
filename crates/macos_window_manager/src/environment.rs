//! Swift-backed environment validation lives on the concrete backend.
//!
//! This module remains as an explicit boundary marker so the crate root can
//! continue to document an environment layer without reintroducing Rust-side
//! probe orchestration.
